---
date: 2026-09-06
type: pre-commit-review
scope: 102 项未提交改动 (90 M + 12 ??)
branch: codex/resume-loop-enhancements
reviewer: software-architect (高见远)
status: 结论 - 可分组提交，无需结构性返工；建议合并 P1 小修
---

# 2026-09-06 提交前逐文件架构审查（102 项改动）

## 1. 结论摘要

对工作区 102 项改动按逻辑域逐文件审查（后端 46 main + 22 test、web 26 + 3 新、pdf-service 3、docs 3、新增 12 项全部完整读取）。

- **未发现 P0（阻塞提交）问题**：无密钥/真实邮箱/Token 泄漏（历史问题未复发）；Flyway V25 合法；前端-后端契约逐项核对一致（URL/方法/字段/参数）；新增 capability registry 均被真实引用（非文档空谈）。
- **整体判断：架构质量良好，改动高度内聚**，主题为「AI 任务能力/授权注册表统一、账号删除围栏、职业资料证据边界、系统/PDF 健康契约、面试资产可选章节与状态补全、简历确认数组路径稳定性 + AI 快照 PII 脱敏」。
- 报告仅静态审查，未执行 build/test；建议工程师按本报告 P1 修复后在提交前跑全套验证（后端 550+ 定向、web build、pdf 测试）。

## 2. 改动内容归类（逻辑域 → 主要文件）

| 域 | 主题 | 代表文件 |
| --- | --- | --- |
| AI 任务/授权 | 能力注册表 AiTaskCapabilityRegistry + AiTaskConsentPolicy 统一 create/retry/worker/领域端点类别策略；AiProviderRegistry.route 增加 isAvailable 过滤 | `AiTaskCapabilityRegistry/ConsentPolicy`(新)、`AiTaskController/Service`、`TaskExecutionService`、`PromptTemplates`、`AiQuotaService`、`InlineOptimizeController`、`InterviewFollowUpAiService/OperationSupport` |
| 账号删除 | DISABLED+deletedAt、JWT 立即失效、撤回 AI 授权、取消未完成任务、失败导出任务 | `AuthService`、`JwtAuthenticationFilter`、`AiConsentService.withdrawIfGranted`、`AiTaskRepository/ExportTaskRepository`(新增批量)、`ExportTaskWorker`(用户态检查) |
| 资料证据 | 资料须有"来源原文或有效结构化内容"，列表/搜索/详情暴露 evidenceReady，选材/生成排除无证据资料 | `CareerMaterialEvidence`(新)、`CareerMaterialService/Repository/DTO×3`、`JobGenerationService`、`MaterialSelector`、`JobMaterialSelectionService` |
| System/PDF 健康 | health 返回 capability checks(api/ai-provider/pdf-renderer)，pdf-service /health 探活，PDF readiness | `SystemCapabilityRegistry`(新)、`SystemController`、`SystemHealthResponse`、`PdfServiceClient.checkHealth`、`pdf-service/browserPool.js、server.js` |
| 面试/资产 | section_key 可空（V25），素材可独立于章节；状态响应补 interviewMode/outputLanguage，来源/语言漂移修复 | `V25__interview_asset_optional_section.sql`(新)、`InterviewAssetSection/Service`、`InterviewStateResponse/Assembler`、`InterviewPromptContextAssembler` |
| 简历确认/AI 快照 | 数组路径稳定(先删后编不漂移)；素材 AI 快照对所有类型脱敏 PII | `ResumeJsonNormalizer`、`CareerMaterialAiSnapshotSanitizer` |
| 评分/JD/版本 | 读取 wrapped parsed data(修缓存失效)、RuleRegistry 权重校验总和与规则集全量；JD 编辑失效解析、parse 行锁防覆盖；简历版本操作悲观锁 | `ScoringService`、`RuleRegistry`、`JobDescriptionService`、`PersonalProfileService`、`ResumeRepository/Service/VersionService` |
| 前端 | 导航注册表收敛(i18n guard)、resumeSource 标签 util、inline/achievement 幂等键、Export UNKNOWN、interview/evidence 字段、多处 computed i18n 修复 | `navigation/registry.ts`、`utils/resumeSource.ts`(新)、`router`、`AppLayout`、`i18n`、`api/*`、`views/*` |

## 3. 问题清单

### P0（必须阻塞提交）
- 无。

### P1（建议修复后提交）
| # | 位置 | 问题 | 建议 |
| --- | --- | --- | --- |
| P1-1 | `web/src/i18n/index.ts`（resumeDetail 两组） | 新增 `utils/resumeSource.ts` 兜底键 `resumeDetail.sourceOther` 在 **resumeDetail** 命名空间两个 locale 下均不存在（`sourceOther` 只存在于 interview 命名空间）。当前枚举为封闭 5 值不会触发，但未来/异常 sourceType 触发时 UI 将渲染裸 key | 在 zh/en 的 `resumeDetail` 下补 `sourceOther`（与 interview.sourceOther 同文案），并让 check-i18n 覆盖 util 兜底键 |
| P1-2 | `InlineOptimizeController` + `web/src/api/ai.ts` | `/api/ai/inline-optimize`、`/api/ai/achievement-guidance` 由"可省略"改为 **Idempotency-Key 强制(缺失 400)**，属契约破坏型变更；页面调用方已同步，但 e2e 若走 mock route 不影响、若有直连旧调用方会 400 | 提交 message 明示 breaking；发布前确认无其他客户端直连；`ResumeEditorView` 每次打开助手生成新 key（不做同内容去重）符合预期，无需改 |
| P1-3 | 同意类别收紧（AiTaskCapabilityRegistry + AiTaskService/worker/领域端点） | RESUME_OPTIMIZE/INLINE_OPTIMIZE/MATERIAL_IMPORT/ACHIEVEMENT_GUIDANCE/INTERVIEW_COACH 的必需 dataCategories 从 `[]`/宽松改为具体类别；历史 GRANTED 记录若缺类别会在部署后返回 CONSENT_REQUIRED 直到用户重新授权 | 属安全正向收紧。发布说明中提示存量用户可能需重新授权；确认前端收到 40302 时会引导重新授权 |

### P2（建议下轮/接受为已知风险）
| # | 位置 | 问题 |
| --- | --- | --- |
| P2-1 | `CareerMaterialService.list` | 移除了轻量 summary projection（findSummaries），退回 `findByUserIdOrderByUpdatedAtDesc` 全实体读取后再 Java 过滤/算 evidenceReady。与 ideation Risk#1 自述一致，属为 evidenceReady 的有意折中；数据增长后为扩展风险。留意后续按 ideation Idea#1 做"含 raw-evidence 的投影" |
| P2-2 | `CareerMaterialSummary` | 5 参兼容构造器（evidenceReady 默认 true）已无任何调用，死代码，建议删除或等投影恢复时再启用 |
| P2-3 | `JwtAuthenticationFilter` | 每个已认证请求新增一次 `userRepository.findById`（此前仅解析 token），换取"删除/禁用即时失效"。性能/DB 读放大请知悉（W 亦可）；如可接受，保持现状 |

### P3（可选）
| # | 位置 | 问题 |
| --- | --- | --- |
| P3-1 | V23 唯一键 `uq_asset_section_material(asset_id,section_key,material_id)` | section_key 放开 NULL 后，MySQL 唯一索引不约束 NULL，理论上可重复插入 (asset,NULL,material)；当前应用层 replaceSections 去重且 delete-then-insert，无实际风险。已知即可 |
| P3-2 | `CareerMaterialAiSnapshotSanitizer` | 对所有素材类型按 key 命中即整体丢弃整条（email/phone/url 等）；对 AI 快照属隐私正向，但 key 过滤可能误删业务键（如含 'url' 的自定义字段），与文本占位符脱敏语义不同。建议保持并记录 |
| P3-3 | `InterviewView/CompareVersions/ResumeDetail` 等 | `sectionLabels` 由静态对象改为 computed（响应式 i18n 修复），与 ideation 语言切换审计一致；本轮无 e2e 断言这些文本，建议下轮补语言切换回归 |
| P3-4 | `docs/ideation/…md` | 266KB 文档内含本机绝对临时报告路径（`C:\Users\lby0403\AppData\Local\Temp\architecture-review-*.html`）及开发机用户名；建议改为相对/占位表述或以单独 docs commit 提交 |
| P3-5 | `GenerationConfirmView` / `aiTask` store | SUCCESS + 历史 null confirmationStatus 经服务端归一化后不再进入恢复流程；仅影响本特性上线前遗留的任务，无实际数据影响 |
| P3-6 | `CareerMaterialService.update`/`create` | 仅标题无证据的旧行将无法改名/保存（测试明示此意图）；UI 有 evidenceMissing 引导，删除仍可用，体验上对旧数据偏严但属特性设计 |

### W（观察项，无需改）
- W1 pdf-service `/health` 首次调用会触发一次 Chromium 启动（checkReadiness 走 getBrowser），并在浏览器 disconnect/close 后由下一次 /health 重新拉起；作为探活有副作用，量小可接受。
- W2 `checkHealth` 每次打进系统 health，读超时 1s；pdf-service 宕机时 /api/system/health 会增加至多 1s 延迟，可接受。
- W3 `MaterialSelector`/`JobMaterialSelectionService` 对"无证据资料"静默降级排除/强制必用抛错两条路径并存，UI 与之一致。
- W4 AiTaskConsentPolicy 相对旧 worker 的 JD 检测更宽（含嵌套 input、jdText/targetJdText/jdContext），执行期类别可能比历史创建时更严，仅影响跨版本在途任务。

## 4. 一致性/契约核对结论（重点项）

- **V25 迁移**：语法与既有 V18/V19/V20 的 MODIFY COLUMN 一致（H2 MySQL mode 已用同语法）；命名与 V24 衔接正确；将历史 `''` sentinel 归一为 NULL；不会破坏已有数据。
- **InterviewAssetSection 实体** `section_key` 已去 NOT NULL，与 V25 及新代码（null 而非 ''）对齐。
- **前端-后端契约**：ai.ts（inlineOptimize/guideAchievement 加 key、ConfirmationStatus 类型、guideAchievement 返回值修正为 AiTask）、interview.ts（interviewMode/outputLanguage）、system.ts（status/checks）、export.ts（UNKNOWN 兜底）、careerMaterial.ts（evidenceReady）均与 Controller/DTO 匹配。
- **导航/路由**：navigation/registry.ts 与 router 启动校验联动；check-i18n 覆盖 registry 键双语；labelKey 命名与 i18n 目录一致。
- **新类均被真实引用**：AiTaskCapabilityRegistry（controller/service/worker/quota/prompt/测试）、AiTaskConsentPolicy（service/worker/面试服务）、CareerMaterialEvidence（service/select/generate）、SystemCapabilityRegistry（controller）——无"文档写了实现没接"问题。
- **隐私扫描**：git diff 与新增文件均未发现真实邮箱/密钥/Token/口令；PII 脱敏逻辑有单测。

## 5. 提交分组方案（建议 10 个语义化 commit，按序执行）

依赖要点：C9（web 全量）须在 C1/C3/C4/C5（后端契约）之后；其余相互独立，可按域先行。

| 组 | message 主题 | 文件（仅列出新增/代表） |
| --- | --- | --- |
| C1 | fix(ai-task): capability & consent registry 统一任务类别策略（含 inline/achievement Idempotency-Key 强制） | AiTaskCapabilityRegistry/ConsentPolicy(新)、AiTaskController/Service、TaskExecutionService、AiQuotaService、PromptTemplates、InlineOptimizeController、InterviewFollowUpAiService、InterviewOperationSupport；测试 AiTaskCapabilityRegistryTest/AiTaskServiceTest/AiTaskControllerIT/TaskExecutionServiceTest/DatabaseTaskWorkerIT/InlineOptimizeControllerIT |
| C2 | feat(auth): 账号删除围栏 - 禁用、JWT 即时失效、撤回授权、取消 AI/导出任务 | AuthService、JwtAuthenticationFilter、AiConsentService、AiTaskRepository、ExportTaskRepository、ExportTaskWorker；测试 AuthServiceTest/AuthControllerIT/AiConsentServiceTest/ExportTaskWorkerTest(新) |
| C3 | feat(evidence): 职业资料证据边界(后端) | CareerMaterialEvidence(新)、CareerMaterialService/Repository/DTO×3、JobGenerationService、MaterialSelector、JobMaterialSelectionService；测试 CareerMaterialServiceTest/CareerMaterialControllerIT/JobMaterialSelectionServiceTest |
| C4 | feat(health): 系统能力健康检查 + PDF readiness（含 pdf-service） | SystemCapabilityRegistry(新)、SystemController、SystemHealthResponse、AiProviderRegistry、PdfServiceClient、pdf-service/src/server.js、browserPool.js、browser-pool.test.js；SystemControllerTest(新) |
| C5 | fix(interview): 答案资产可选章节(V25) + 状态补模式/语言 | V25 SQL(新)、InterviewAssetSection/Service、InterviewStateResponse/Assembler、InterviewPromptContextAssembler；测试 InterviewAssetServiceTest/InterviewStateAssemblerTest/InterviewPromptContextAssemblerTest |
| C6 | fix(resume): 确认数组路径稳定 + AI 素材快照 PII 脱敏 | ResumeJsonNormalizer、CareerMaterialAiSnapshotSanitizer；测试 ResumeJsonNormalizerTest/CareerMaterialAiSnapshotSanitizerTest |
| C7 | fix(scoring): 读取 wrapped JD 解析缓存 + 规则集全量加权 | ScoringService、RuleRegistry；测试 ScoringServiceTest/RuleRegistryTest(新) |
| C8 | fix(resume-loop): JD 解析失效/行锁、profile objective、简历版本悲观锁 | JobDescriptionService、PersonalProfileService、ResumeRepository/Service/VersionService；测试 JobDescriptionServiceTest/PersonalProfileServiceTest |
| C9 | feat(web): 导航注册表+i18n guard+来源标签+幂等/UNKNOWN/evidence UI | navigation/registry.ts、utils/resumeSource.ts(新)、router、AppLayout、i18n、check-i18n*.mjs、api/{ai,careerMaterial,export,interview,system}.ts、stores/aiTask、views/*、components/career-material/*（含 P1-1 修复） |
| C10 | docs: 本地验证/README/ideation 归档 | docs/LOCAL_VALIDATION.md、docs/README.md、docs/ideation/(新，建议附 P3-4 清理) |

可选合并：C7+C8 可并为 fix(scoring/jd/version) 一个 commit；C6 亦可并入 C3 提交链。默认按上表执行，每个 commit 独立可编译（C9 需后端已并入）。
