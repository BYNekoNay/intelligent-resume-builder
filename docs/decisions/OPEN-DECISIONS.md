# 悬而未决登记册（OPEN-DECISIONS）

> 铁律：**只追加 + 就地关闭**。OPEN → RESOLVED 时补 `Resolution` 字段，不删除原条目。
> 已关闭项可升格为 ADR。

| Date | Source | Open Item | Related Constraints | Current Leaning | Blocked By | Resolves When | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-23 | plans/003 (I2) | ⚠️ **严重度已降级**（ADR-005 使长任务从 477s 降到 ~7s，队首阻塞大幅缓解；串行本身仍在，多用户场景仍需修）<br>AI worker 按 id 串行领取，长任务（生成 477s）会饿死短任务（实测内联润色平时 13s 被饿死 8 分钟、`retryCount=0`） | `DatabaseTaskWorker` 每次 `claimBatch(owner,1)`；`claimableTasks` 为 `FOR UPDATE` + `ORDER BY id`；租约由 heartbeat 每 60s 续期 | 方案 A：按任务类型分组领取，短任务可插队；B（提高 worker 并发）待压测后定 | 需先确认容量预期（内存/DB 连接池/JVM 1G 上限） | 容量评估完成并选定 A/B 后 | OPEN · `waiting-on-external-condition` |
| 2026-09-23 | plans/003 (I3) | ~~生成任务每次在链首白等 300s 后顺延，总耗时 477s~~ | ~~`BAILIAN_READ_TIMEOUT_S=300`~~ | 诊断结论：**根因是推理开销**（88% 输出 token 为不可见 reasoning），非 prompt 规模亦非 provider 阻塞。a/b/c 三方案均不适用，改采方案 d（关闭推理） | — | ✅ 已关闭：线上实测 477s → **7s**，链首直接成功 | **RESOLVED** (2026-09-24)<br>Resolution: 对 `JOB_GENERATION` 关闭推理（`enable_thinking=false`），并配套把 400 改为顺延。见 ADR-005 |
| 2026-09-24 | ADR-005 §7.4 | `kimi-k3` 拒绝 `temperature` 参数，在本应用的任何请求下都必然 400，是链上纯失败跳转节点 | `BAILIAN_MODEL_CHAIN` 为环境变量；本应用的每次请求都带 `temperature` | 建议直接从链中移除（保留它只增加一次约 1s 的失败往返） | 需运维在部署机修改 `.env` 并重启 | 从 `BAILIAN_MODEL_CHAIN` 移除后 | OPEN · `waiting-on-external-condition` |
| 2026-09-23 | plans/003 (I4) | 模型链告警阈值 `available <= 2` 是否合适 | 默认链长 8，额度类冷却 30 分钟 | 取 2/8 ≈ 25% 余量作 early warning；实测告警触发频率后可能调整 | 需生产运行数据 | 观察一个配额周期（约 1 个月）后 | OPEN · `design-decision-to-evaluate` |
| 2026-09-23 | plans/003 §5 | 测试环境 `8.160.165.227` 的定位（现有 5 个用户全部为测试账号） | 直连部署（非 Docker）、无数据隔离 | 明确定位为**可随时重置的测试环境**并在部署手册声明（本次 S6 已补充声明） | — | ✅ 已关闭：见 DEPLOYMENT_DIRECT.md 环境属性说明 | **RESOLVED** (2026-09-23)<br>Resolution: 在部署手册中声明该环境属性与测试账号前缀，不再按准生产对待 |
| 2026-09-23 | plans/003 (I5) | 决策缺少可追溯登记册 | 此前决策只存在于 commit message 与评审文档 | 建立 `docs/decisions/` + ADR + 本登记册 | — | ✅ 已关闭：本目录建立并补录 ADR-001~004 | **RESOLVED** (2026-09-23)<br>Resolution: 新建 docs/decisions/，补录 4 条 ADR 并立本登记册 |
| 2026-09-25 | ADR-006<br>E2-judgment-table-v2 §11 | **K7「待复核红线」**：2 组内容独立的真实锚点上 Arm N 均比 Arm R 低 9–10 分（`real-6 d=−10`、`real-22 d=−9`，均 `|d|>δ=8`），指向"关推理在**真实输入**上可能有实质退化" | 输入层配对差噪声 ≈7 分 vs `δ_eff=8`（`real-6` 与 `real-11` 内容等价却差 7 分）；评分量表压在 88–100 近天花板；两个候选裁判均未过 twin 校准 → 量具不足以定论 | 倾向"**确有退化信号，但不可据此下结论**"；**禁止**表述为"已证明退化" | 需可信评分者 + 更多内容互不相同的真实锚点 + 重估 δ/量表（第二轮三件必要条件） | 第二轮以可信工具重验 `real-6` 与 `real-22` 后 | OPEN · `design-decision-to-evaluate` |
| 2026-09-25 | ADR-006 §6 | ATS 分析的 **600s 读超时尾部未消除**（实测 Arm R 1/12 撞满 600.102s；直连中位 97s、生产任务级 212–405s），且本轮判定**保留推理**（不解除慢） | `BAILIAN_READ_TIMEOUT_S=300`、链总预算 600s；ADR-006 未纳入关闭推理名单 | 做**不依赖本实验**的短期缓解：超时尾部治理 + 可用性兜底 | 需先明确"超时后用户应看到什么"的产品口径 | 产品口径确定后 | OPEN · `waiting-on-external-condition` |
| 2026-09-24 | E3-F1<br>`ats-reasoning-eval/.workbuddy/tmp/ats-eval/finding-synonym-dictionary.md` | **同义词词典在运行时未生效（按空词典行为）**，导致多词关键词（如 `Spring Boot`）因 tokenizer 按空格切词而**必然判 MISSING**；实测 `keywordCoverage` 被系统性低估（60 应为 80、20 应为 40）—— **用户看到的 ATS 规则分比其应得的更低** | `app.scoring.synonym-dictionary`（`application.yml` L206–213）配置存在但实测对不上；`Normalizer` 同义词归一 与 tokenizer 空格切分歧义；无单元测试覆盖"配置词典生效" | 修法待定：① 让配置词典真正生效（同时修 tokenizer 对多词关键词的处理）② 或明确废弃该配置并移除 ③ 至少补一条"配置词典生效"的测试 | 本轮**刻意冻结** —— 修了会改变生产行为、污染正在进行的 ATS 推理对比实验基线 | 该实验收口后（结论已落盘，不再需要冻结基线） | **RESOLVED** (2026-09-25)<br>Resolution: **采选项 ①（并顺带做了 ③）**。修复三处同源缺陷：**A** 词典绑定失效 —— `@Value("#{${...}}")` 对嵌套 YAML 无效，改由 `@ConfigurationProperties` 承载（新增 `ScoringProperties`），并**立为 ADR-008**；**B** `ResumeKeywordExtractor` 只读 `skills[*].keywords`，而规范字段是 `items`（据 `JobGenerationPromptBuilder` 示例）；**C** PDF 模板只渲染 `skill.name`，导致技能项被整批丢弃。另在 `Normalizer` 增加**连续词组匹配**（`tokenizeWithPhrases`），使未进词典的多词关键词也能命中，并**全面禁用**该 SpEL 绑定写法。真实环境复测：`keywordCoverage` 20.0 → **40.0**、`skillCoverage` 20.0 → **40.0**，"建议补充 Spring Boot"的误导性建议消失，而真正缺失项仍正确判缺失。回归：后端 **652 测试全绿**、PDF 服务 **24 全绿**、非 AI 链路 27 项全通过。见 [修复报告](../reviews/2026-09-25-ats-keyword-coverage-fix.md) |
| 2026-09-25 | ADR-007 /<br>`DEPLOYMENT_DIRECT.md` §6 AC-14 | **腾讯云安全组未放行 TCP 8088**：服务器侧 nginx 已在 `0.0.0.0:8088` 正常服务（本机 `curl` → 200），但公网 `http://101.35.239.218:8088/` **连接超时**，即当前智历**没有可用的公网入口** | nginx 监听 8088；安全组是云侧规则，服务器内部无法自查或修改；本机存在全局代理会干扰探测（须 `--noproxy '*'`） | 在控制台添加放行规则 TCP:8088 | 需用户登录腾讯云控制台操作 | 放行后，从**绕过代理**的客户端复测 `http://101.35.239.218:8088/` 得到 200 | **RESOLVED** (2026-09-25)<br>Resolution: 已放行；公网实测 `GET /` → 200、`/api/system/health` → 200，且公网侧完整功能测试 58 项通过 |
| 2026-09-25 | `reviews/2026-09-25-cloud-functional-test.md` §6 | **E3-F1 在云环境复现，证据比原条目更精确、影响边界已收敛**：同一份简历与 JD **逐字都含 `Spring Boot`**，仍被判 `缺少 JD 关键词: Spring Boot`；`keywordCoverage = 20.0`（**正确应为 40.0**）。**新收敛的边界**：单 token 关键词（`Java`）判断**正确**，只有**含空格的多词关键词必然误判**；真正缺失的 `MySQL`/`Redis` 判断也**正确** —— 即不是"关键词全判错" | 同 E3-F1；`application.yml` L206–213 的 `synonym-dictionary` 配置齐全（含 `spring: ["spring","spring boot","spring cloud"]`）但运行时未生效 | 修法仍三选一（见 E3-F1 原条目） | ~~冻结基线~~ **该阻塞条件已消失**：ATS 推理对比实验已收口（E5 报告 + ADR-006），不再需要冻结 | 选定评分口径并补回归用例后 | **RESOLVED** (2026-09-25)<br>Resolution: 已按选项 ① 修复并在真实环境验证 —— 同一份数据的 `keywordCoverage` 由 20.0 变为 **40.0**，与"20 应为 40"完全吻合。详见 E3-F1 原条目的 Resolution 与 [修复报告](../reviews/2026-09-25-ats-keyword-coverage-fix.md) |
